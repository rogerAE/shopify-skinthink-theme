{% style %}
  .rotating-block-{{ section.id }} {
    padding: {{ section.settings.padding_top }}px {{ section.settings.padding_sides }}px {{ section.settings.padding_bottom }}px;
    margin: 0 auto;
    max-width: {{ section.settings.max_width }}px;
  }

  .rotating-block-{{ section.id }} .section-heading {
    font-size: {{ section.settings.heading_size }}px;
    color: {{ section.settings.heading_color }};
    text-align: {{ section.settings.text_alignment }};
    margin: 0 0 20px 0;
    line-height: 1.2;
    font-weight: {{ section.settings.heading_weight }};
  }

  .rotating-block-{{ section.id }} .section-subheading {
    font-size: {{ section.settings.subheading_size }}px;
    color: {{ section.settings.subheading_color }};
    text-align: {{ section.settings.text_alignment }};
    margin: 0 0 40px 0;
    line-height: 1.5;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
  }

  .rotating-block-{{ section.id }} .rotating-container {
    background: {{ section.settings.rotating_bg_color }};
    padding: 40px;
    border-radius: 4px;
    margin: 20px 0;
    text-align: center;
    position: relative;
    min-height: 200px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .rotating-block-{{ section.id }} .rotating-content {
    position: relative;
    width: 100%;
  }

  .rotating-block-{{ section.id }} .rotating-item {
    width: 100%;
    text-align: center;
    position: absolute;
    top: 0;
    left: 0;
    opacity: 0;
    transition: opacity 0.5s ease-in-out;
    padding: 0 20px;
  }

  .rotating-block-{{ section.id }} .rotating-item.active {
    opacity: 1;
    position: relative;
  }

  .rotating-block-{{ section.id }} .rotating-item-heading {
    font-size: {{ section.settings.rotating_heading_size }}px;
    color: {{ section.settings.rotating_heading_color }};
    font-weight: {{ section.settings.rotating_heading_weight }};
    margin-bottom: 15px;
    line-height: 1.3;
  }

  .rotating-block-{{ section.id }} .rotating-item-text {
    font-size: {{ section.settings.rotating_text_size }}px;
    color: {{ section.settings.rotating_text_color }};
    line-height: 1.5;
    max-width: 800px;
    margin: 0 auto;
  }

  @media screen and (max-width: 768px) {
    .rotating-block-{{ section.id }} {
      padding: {{ section.settings.padding_top | times: 0.8 }}px {{ section.settings.padding_sides | divided_by: 2 }}px {{ section.settings.padding_bottom | times: 0.8 }}px;
    }
    
    .rotating-block-{{ section.id }} .rotating-container {
      padding: 30px;
      min-height: 180px;
    }
    
    .rotating-block-{{ section.id }} .rotating-item-heading {
      font-size: {{ section.settings.rotating_heading_size | times: 0.8 }}px;
    }
    
    .rotating-block-{{ section.id }} .rotating-item-text {
      font-size: {{ section.settings.rotating_text_size | times: 0.8 }}px;
    }
  }

  @media screen and (max-width: 480px) {
    .rotating-block-{{ section.id }} .rotating-container {
      padding: 20px;
      min-height: 160px;
    }
    
    .rotating-block-{{ section.id }} .rotating-item-heading {
      font-size: {{ section.settings.rotating_heading_size | times: 0.7 }}px;
    }
    
    .rotating-block-{{ section.id }} .rotating-item-text {
      font-size: {{ section.settings.rotating_text_size | times: 0.7 }}px;
    }
  }
{% endstyle %}

<div class="rotating-block-{{ section.id }}">
  {% if section.settings.heading != blank %}
    <h2 class="section-heading">{{ section.settings.heading }}</h2>
  {% endif %}
  
  {% if section.settings.subheading != blank %}
    <div class="section-subheading">{{ section.settings.subheading }}</div>
  {% endif %}
  
  <div class="rotating-container">
    <div class="rotating-content">
      {% for block in section.blocks %}
        <div class="rotating-item {% if forloop.first %}active{% endif %}" data-index="{{ forloop.index0 }}">
          {% if block.settings.heading != blank %}
            <h3 class="rotating-item-heading">{{ block.settings.heading }}</h3>
          {% endif %}
          {% if block.settings.text != blank %}
            <div class="rotating-item-text">{{ block.settings.text }}</div>
          {% endif %}
        </div>
      {% endfor %}
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const rotatingItems = document.querySelectorAll('.rotating-block-{{ section.id }} .rotating-item');
    let currentIndex = 0;
    
    if (rotatingItems.length > 1) {
      setInterval(function() {
        rotatingItems[currentIndex].classList.remove('active');
        currentIndex = (currentIndex + 1) % rotatingItems.length;
        rotatingItems[currentIndex].classList.add('active');
      }, {{ section.settings.rotation_speed }});
    }
  });
</script>

{% schema %}
{
  "name": "Rotating Content Block",
  "settings": [
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "range",
      "id": "max_width",
      "min": 600,
      "max": 1200,
      "step": 50,
      "unit": "px",
      "label": "Max Width",
      "default": 1000
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 20,
      "max": 200,
      "step": 5,
      "unit": "px",
      "label": "Top Padding",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 20,
      "max": 200,
      "step": 5,
      "unit": "px",
      "label": "Bottom Padding",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_sides",
      "min": 20,
      "max": 100,
      "step": 5,
      "unit": "px",
      "label": "Side Padding",
      "default": 40
    },
    {
      "type": "select",
      "id": "text_alignment",
      "label": "Text Alignment",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "right", "label": "Right" }
      ],
      "default": "center"
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "ACNE ISN'T ONE-SIZE-FITS-ALL"
    },
    {
      "type": "richtext",
      "id": "subheading",
      "label": "Subheading",
      "default": "<p>Reactive skin is complex—and the support it deserves should be, too. At Authentic Ego, we don't treat breakouts like a surface issue. We go deeper, developing formulas that work with your skin's biology, not against it.</p>"
    },
    {
      "type": "header",
      "content": "Rotating Block"
    },
    {
      "type": "color",
      "id": "rotating_bg_color",
      "label": "Background Color",
      "default": "#F8F8F8"
    },
    {
      "type": "range",
      "id": "rotating_heading_size",
      "min": 16,
      "max": 48,
      "step": 1,
      "unit": "px",
      "label": "Heading Size",
      "default": 24
    },
    {
      "type": "range",
      "id": "rotating_text_size",
      "min": 14,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Text Size",
      "default": 16
    },
    {
      "type": "color",
      "id": "rotating_heading_color",
      "label": "Heading Color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "rotating_text_color",
      "label": "Text Color",
      "default": "#333333"
    },
    {
      "type": "select",
      "id": "rotating_heading_weight",
      "label": "Heading Weight",
      "options": [
        { "value": "400", "label": "Normal" },
        { "value": "500", "label": "Medium" },
        { "value": "600", "label": "Semi-bold" },
        { "value": "700", "label": "Bold" }
      ],
      "default": "600"
    },
    {
      "type": "range",
      "id": "rotation_speed",
      "min": 2000,
      "max": 8000,
      "step": 500,
      "unit": "ms",
      "label": "Rotation Speed",
      "default": 3000,
      "info": "Time between rotations (2000-8000 milliseconds)"
    }
  ],
  "blocks": [
    {
      "type": "item",
      "name": "Rotating Item",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "BARRIER DYSFUNCTION"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "Text",
          "default": "<p>A compromised barrier can't protect, heal, or glow. Every Authentic Ego formula supports and strengthens your skin's natural defenses.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Rotating Content Block",
      "category": "Text",
      "blocks": [
        {
          "type": "item",
          "settings": {
            "heading": "BARRIER DYSFUNCTION",
            "text": "<p>A compromised barrier can't protect, heal, or glow. Every Authentic Ego formula supports and strengthens your skin's natural defenses.</p>"
          }
        },
        {
          "type": "item",
          "settings": {
            "heading": "HORMONAL IMBALANCE",
            "text": "<p>We address the root causes of hormonal acne with science-backed ingredients that regulate without disrupting.</p>"
          }
        },
        {
          "type": "item",
          "settings": {
            "heading": "MICROBIOME SUPPORT",
            "text": "<p>Our formulas nourish your skin's ecosystem to maintain optimal balance and resilience.</p>"
          }
        }
      ]
    }
  ]
}
{% endschema %}